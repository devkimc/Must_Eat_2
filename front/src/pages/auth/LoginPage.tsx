import React from 'react';
import { LoginContainer } from 'containers';

const LoginPage = () => {
    return (
        <div>
            <LoginContainer />
        </div>
    );
};

export default LoginPage;
